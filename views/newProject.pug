extends layout

include mixins/createProject

block content 
    .contenido-principal
        h1 Projects

        if errors
            each error in errors
                .alerta= error.text
        +createProject(project)