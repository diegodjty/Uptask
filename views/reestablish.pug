<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login 
        .contenedor-formulario 
            h1= pageName 
            p Add email to reestablish your password 
            if locals.messages
                .alertas 
                    each categoria in Object.keys(locals.messages)
                        each error in messages[categoria] 
                            .alerta(class=`${categoria}`)
                                p= error
            form.caja-login(method='POST' action='/reestablish')
                .campo 
                    label(for="email") E-mail: 
                    input(type="email" name='email' placeholder='Your Email' required value= email)
                .campo.enviar 
                    input.boton(type='submit' value='Reestablish Password')
                .campo-acctiones 
                    a(href="/login") Login