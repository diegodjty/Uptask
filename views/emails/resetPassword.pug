doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style 
            include ./email.css
    body 
        h2 Reset Password 
        p Hi, click here to reset your password, this link is temporary if its expired request a new one
        a(href=`${resetUrl}`) Reset Password 
        p If you can't access this link go to #{resetUrl}
        p If you didn't ask for this email, ignore it.