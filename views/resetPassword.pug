<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login 
        .contenedor-formulario 
            h1= pageName 
            p Add your new your password 
            //- if locals.messages
            //-     .alertas 
            //-         each categoria in Object.keys(locals.messages)
            //-             each error in messages[categoria] 
            //-                 .alerta(class=`${categoria}`)
            //-                     p= error
            form.caja-login(method='POST')
                .campo 
                    label(for="password") Password: 
                    input(type="password" name='password' placeholder='Your Password' required value= email)
                .campo.enviar 
                    input.boton(type='submit' value='Reestablish Password')